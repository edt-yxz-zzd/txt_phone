deprecated
    NO Warning:
        File -> Path
        awt -> swing -> javafx
        Enumeration -> Iterator
        ?? Iterable -> Stream

Class Hierarchy
    are there concept differences between ArrayList and Vector?
    why not Stack super Vector but Stack extends Vector?????????

    why not Iterator extends Iterable??
detect
    is a given file a image? if yes, params of the image (e.g. width, height)?
    what is the charset of a given html file?

deprecated primitives
    Java Thread Primitive Deprecation
    Thread.stop, Thread.suspend and Thread.resume Deprecated?.

lock:
    mutex.lock();
    // what if throw here??
    try { ...
    } finally {
        mutex.unlock();
    }
