
no side effect
    what?
        time and memory are consumed
        what aspect has not been affected?
    copy constructor, move constructor should be no side effect in some sence, so that the compile can do NRVO/RVO;
no side effect print/log // IO
    may or may not do print
    where to print?


