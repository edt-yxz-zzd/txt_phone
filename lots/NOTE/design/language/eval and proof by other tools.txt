
#call "yyy.cfg" definition
    // yyy.cfg
    //  let definition = sys.argv[1] # %1 # the input
    //  let cache = "xxx.src"
    //  if not_exist(cache)
    //      data, proof = eval(definition)
    //      write(cache, data, proof)
    //  else:
    //      data, proof = read cache
    //  return data, proof


