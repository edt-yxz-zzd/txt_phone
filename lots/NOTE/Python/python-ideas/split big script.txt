
ideas:
    "relative import" in script depends on filesystem
why?
    assume script may be move to anywhere
    if we split a big script into smaller scripts
        , we have to use "relative import"

